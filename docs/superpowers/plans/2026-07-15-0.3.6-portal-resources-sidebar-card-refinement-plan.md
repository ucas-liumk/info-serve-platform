# 0.3.6 资料共享栏目与卡片精修实施计划

对应设计：`docs/superpowers/specs/2026-07-15-0.3.6-portal-resources-sidebar-card-refinement-design.md`

实施分支：`feat/0.3.6-portal-home-toolbar`

正式编辑与重型验证目录：`E:\gallant-dev\active\info-serve-0.3.6-home`

## Task 1：同步 Windows 检查点并记录基线

1. 在 Windows 检查工作树无未提交变更，记录 `BASE_COMMIT`。
2. 先尝试 `git fetch origin` 和 fast-forward；GitHub 连接失败时，从 Mac 当前分支生成 git bundle，在 Windows 通过 bundle fast-forward，同步到设计提交。
3. 确认 Windows 与 Mac `git rev-parse HEAD` 一致，且仍在 `feat/0.3.6-portal-home-toolbar`。
4. 确认 17018 开发服务可访问，运行实现前资料页截图：

       set "UI_CAPTURE_BASE=http://127.0.0.1:17018"
       set "UI_CAPTURE_OUT=output\ui-capture-resources-refinement-before"
       python deploy\scripts\ui-capture.py

5. 保存 1440px 网格截图作为左栏、顶栏、三列卡片旧基线。

## Task 2：先补栏目行为测试

文件：

- 修改 `plus-ui/src/views/portal/resources/categoryFacets.test.ts`
- 仅在需要纯函数时修改 `plus-ui/src/views/portal/resources/categoryFacets.ts`
- 修改 `plus-ui/src/views/portal/resources/components/ResourceFilterPanel.vue`

步骤：

1. 先添加失败测试，覆盖：
   - 无分类选择时“全部资料”为选中态。
   - 点击“全部资料”只发出空的分类数组。
   - 分组按钮具有 `aria-expanded`、稳定 `aria-controls`，对应内容 id 存在。
   - 长栏目名称渲染完整，数量为独立内容。
2. 运行目标测试确认先失败。
3. 实现“全部资料”入口、分组稳定 id、独立数量胶囊和两行栏目标签。
4. 不推算全部资料数量；分组与栏目计数继续使用接口返回值。
5. 再运行目标测试确认通过。

## Task 3：实现贯穿式左栏两层结构

文件：

- 修改 `plus-ui/src/views/portal/resources/components/ResourceSidebar.vue`
- 修改 `plus-ui/src/views/portal/resources/components/ResourceFilterPanel.vue`
- 修改 `plus-ui/src/views/portal/resources/index.vue`

步骤：

1. `ResourceSidebar.vue` 增加外层连续表面和内层 sticky 容器：
   - 外层由页面 grid stretch 到右侧内容底端，不设置 viewport `max-height`。
   - 内层负责 sticky、品牌、返回首页、栏目错误和唯一栏目滚动区。
2. 桌面左栏宽度设为 264px 对应的现有合法尺寸表达；页面间距只用设计令牌允许档位。
3. 将 disclosure 断点从 980px 调整为 1120px，并同步 `matchMedia`、CSS media query 与可访问行为。
4. 1120px 以下保留现有展开后聚焦、Escape 收起和焦点归还。
5. 使用既有 `--ip-*` 表面、边框、强调、圆角、焦点和阴影令牌，不新增颜色。

## Task 4：收紧资料工具栏

文件：

- 修改 `plus-ui/src/views/portal/resources/components/ResourceHeader.vue`

步骤：

1. 桌面重排为“动态标题/数量—搜索—页面操作”三段单行结构，目标高度约 76px。
2. 保留现有动态标题：无分类、单分类、多分类显示逻辑不变。
3. 搜索继续是唯一 primary；上传资料、我的资料和通知/用户入口层级不变。
4. 1121–1320px 允许紧凑排列；空间不足时显式换行，不遮挡、不横向滚动。
5. 不改变 Enter 搜索、清空搜索、登录检查、上传和我的资料事件。

## Task 5：重构文件卡片并清理触及样式的硬编码

文件：

- 修改 `plus-ui/src/views/portal/resources/components/ResourceCard.vue`

步骤：

1. 保留 PDF/Office/图片缩略图、缓存、超时和失败负缓存逻辑，只调整展示模板与样式。
2. 四列窄卡使用约 74×106px 的文档舞台；卡片高度允许在 132–152px 内由内容决定。
3. 内容顺序为栏目、两行标题、元数据、操作；删除制造大空白的固定行分配。
4. 三个操作始终保留可访问名称：
   - 卡片内容宽度足够时显示预览/下载文字。
   - 内容宽度不足 280px 时只显示图标，但保留 `aria-label` 与 `title`。
5. 清理本次触及的 fallback/卡片硬编码：`#fff`、硬编码渐变、任意字号、`0.16s ease`，全部映射到 `tokens.scss` 现有令牌或合法 `color-mix()`。
6. hover 不改变布局；`prefers-reduced-motion` 下取消位移。

## Task 6：实现 4/3/2/1 网格与匹配骨架

文件：

- 修改 `plus-ui/src/views/portal/resources/index.vue`
- 修改 `plus-ui/src/views/portal/resources/components/ResourceLoadingSkeleton.vue`

步骤：

1. 网格断点：
   - `>1320px` 四列。
   - `981–1320px` 三列。
   - `768–980px` 两列。
   - `<768px` 单列。
2. 1440/1024/768/375 分别验收 4/3/2/1；所有列使用 `minmax(0, 1fr)`。
3. 骨架屏复用相同列数和卡片比例，防止加载完成时布局跳动。
4. 使用最长合法中文标题、三位数浏览/下载统计、最长文件大小和三个操作做边界检查。

## Task 7：静态验证与目标测试

在 Windows `plus-ui` 目录运行：

    npm run lint:eslint -- src/views/portal/resources/index.vue src/views/portal/resources/categoryFacets.ts src/views/portal/resources/categoryFacets.test.ts src/views/portal/resources/components/ResourceHeader.vue src/views/portal/resources/components/ResourceLoadingSkeleton.vue src/views/portal/resources/components/ResourceCard.vue src/views/portal/resources/components/ResourceSidebar.vue src/views/portal/resources/components/ResourceFilterPanel.vue
    npm run test -- src/views/portal/resources/categoryFacets.test.ts src/views/portal/resources/resourceRequestState.test.ts
    npm run design:audit
    npm run build:prod

同时运行：

    git diff --check
    git grep -n -E '^(<<<<<<<|=======|>>>>>>>)' -- plus-ui/src/views/portal/resources

若测试框架不支持 Vue 组件挂载测试，保留纯逻辑单测，并用 Playwright 对 `aria-controls`、点击“全部资料”和焦点行为做同等自动化验证，不以跳过代替验证。

## Task 8：浏览器与接口验收

1. 运行实现后官方截图：

       set "UI_CAPTURE_BASE=http://127.0.0.1:17018"
       set "UI_CAPTURE_OUT=output\ui-capture-resources-refinement-after"
       python deploy\scripts\ui-capture.py

2. 另用浏览器固定视口拍摄 375/768/1024/1440 网格：
   - 1440 四列、左栏贯穿、紧凑顶栏。
   - 1024 三列、顶部 disclosure。
   - 768 两列。
   - 375 单列且核心操作至少 44px。
3. 检查 200% 文本缩放、长分类名称、无横向溢出、reduced-motion。
4. 检查无分类、单分类、多分类动态标题。
5. 点击“全部资料”后确认只清分类，其他筛选保留。
6. 检查搜索、四组筛选、网格/表格切换、分页、预览、下载、收藏、上传、我的资料。
7. 检查接口：

       curl.exe -i http://127.0.0.1:17018/dev-api/infoservice/portal/resources
       curl.exe -i http://127.0.0.1:17018/dev-api/infoservice/portal/resources/category-tree
       curl.exe -i http://192.168.8.10:17018/dev-api/infoservice/portal/resources
       curl.exe -i http://192.168.8.10:17018/dev-api/infoservice/portal/resources/category-tree

代理 500、连接拒绝或超时均视为失败。

## Task 9：提交、同步与版本控制

1. 确认所有修改文件小于 800 行。
2. 检查：

       git log origin/main..HEAD
       git diff origin/main...HEAD
       git status --short

3. 使用 conventional 中文提交，例如：

       git commit -m "feat: 精修资料栏目与四列卡片"

4. 推送 `feat/0.3.6-portal-home-toolbar`，确认 Mac、Windows 与远端 HEAD 一致。
5. 不创建 PR；等待其余 0.3.6 页面样式完成后统一创建 PR。
