# build outputs
**/target/
plus-ui/node_modules/
plus-ui/dist/
plus-ui/.eslintcache
# runtime data & logs（仅根目录；不加 / 前缀会误伤任意层级同名目录，曾吞掉 plus-ui/src/api/system/dict/data）
/data/
/logs/
# local test traces, screenshots and generated reports
/.playwright-cli/
/output/
/screenshots/
# local release bundles; publish binary packages outside the source repo
/releases/
/release/
# root-level visual scratch files
/*.png
pics/1782179392709.jpg
pics/resurces.png
# generated initdb (regenerate via deploy/scripts/generate-initdb.py)
deploy/initdb-mysql/
deploy/initdb-postgres/
# SDD scratch ledger
.superpowers/
# local editor settings
.vscode/
# misc
.DS_Store
*.class
# 防误提交：工具误产出的目录与 UTM 挂载点
undefined/
.utm-disk/
