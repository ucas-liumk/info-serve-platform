# 开发学习手册

## 目标

记录我们在这个项目中一步一步学习、运行、修改和理解的过程，最终形成一份可重复使用的操作手册。

## 当前时间

- 2026-06-27

## 已完成内容

1. 了解项目是一个基于 Vue 3 + TypeScript + Vite + Element Plus 的前端项目。
2. 看到项目的入口文件和基本目录结构。
3. 已完成依赖安装：
   - 命令：`npm install --registry=https://registry.npmmirror.com`
   - 结果：安装成功。
4. 已成功启动开发服务：
   - 命令：`npm run dev`
   - 结果：Vite 启动成功，访问地址为 `http://localhost:80/`

## 项目基本信息

- 项目名：ruoyi-vue-plus
- 技术栈：Vue 3、TypeScript、Vite、Element Plus
- 运行命令：`npm run dev`
- 访问地址：`http://localhost:80/`

## 下一步计划

1. 启动项目并确认是否能在浏览器中打开。
2. 了解项目的主入口文件和路由结构。
3. 从一个最简单的页面或功能开始学习。

## 操作记录

### 2026-06-27

- 先查看了项目的 package.json 和 README，确认项目是前端项目并且使用 Vite。
- 已完成依赖安装。
- 已手动执行 `npm run dev`，成功启动开发服务。
- 当前访问地址为 `http://localhost:80/`。
- 了解了项目的启动顺序：
  1. `src/main.ts` 负责创建 Vue 应用、挂载插件、注册路由和状态管理。
  2. `src/App.vue` 是整个应用的根组件，里面通过 `router-view` 来显示当前页面。
  3. `src/router/index.ts` 定义了当前页面对应的地址和组件。
- 接下来将继续学习布局页面与侧边栏是如何渲染出来的。
